# 2026-09-07 工作日志

## leetcode 统一测试
- 确认 leetcode 目录文件形态：`array/`、`prove/` 为纯 `class Solution`（无 include、无 main）；`sort/` 混合（13 个有 main，其余纯 Solution）。
- 用户选择用 `leetcode/test.cpp` 作为统一测试入口，通过 `#include "array/49.groupAnagrams.cpp"` 直接包含源文件来测试。
- 约定：test.cpp 顶部需先 include 标准头文件（vector/string/unordered_map/algorithm/iostream）+ `using namespace std;`，再 include 源文件（因为题解源文件里是裸的 vector/string，无 std:: 前缀、无头文件）。
- 为 49.groupAnagrams 生成 3 个题面用例的测试，用 `normalize`（组内排序 + 组间排序）做顺序无关比较。
- 本机无 g++/cl/clang++，wsl 被沙箱黑名单阻止，无法本地编译验证；编译命令见回复。
- 追加耗时测量：用户手动加了 `while(n--)` 循环跑 30 次测性能；改为 `<chrono>` 计时，每个用例累计耗时、最后打印 avg us/次 + 总耗时 ms。计时只包 `groupAnagrams()` 调用本身，normalize/比较在计时区外。
